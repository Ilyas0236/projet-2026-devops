package com.wydad.digital.sports.enums;

/**
 * Catégories sportives du club — uniques et partagées par toutes les
 * disciplines (football, basketball, handball, autre). Alignées sur les
 * catégories sollicitables à l'inscription (auth-service CATEGORIES_VALIDES).
 */
public enum Category {
    U15, U17, U18, U20, SENIOR
}
